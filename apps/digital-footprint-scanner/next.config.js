/** @type {import('next').NextConfig} */
const nextConfig = {
  // The turbopack option is not directly supported in Next.js 14.2.5.
  // If you need to configure Turbopack, use the experimental flag:
  // experimental: {
  //   turbo: {
  //     root: process.cwd(),
  //   },
  // },
};

module.exports = nextConfig;